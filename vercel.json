{
    "builds": [
      {
        "src": "nuxt.config.js",
        "use": "@nuxtjs/vercel-builder",
        "config": {
          "serverFiles": ["api/**","store/**"]
        }
      }
    ],
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
    
  }
  